# Test reports

This directory holds **generated** varbase-e2e (Playwright + Cucumber-js) reports
produced on every test run (`cucumber_report.json` / `.html` / `.pdf`). They are
recreated on each run and must **not** be committed — `.gitignore` ignores everything
here except this `README.md`, which keeps the directory in the repository.
