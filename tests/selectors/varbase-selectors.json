{
  "$comment": "Named selectors ported from the Varbase profile Behat suite (tests/selectors/varbase/*.yml): back-end-selectors.yml, front-end-selectors.yml, installation-selectors.yml. Registered by the webship-js selector registry (worldParameters.selectors.files). CSS and XPath selectors are namespaced under `css` / `xpath`.",
  "css": {
    "Primary tabs": "#navigation .tabs.primary",
    "Primary tabs first link": "#navigation .tabs.primary > ul > li:nth-child(1) > a",
    "copyright": "footer#footer .copyright",
    "branding": "#branding"
  },
  "xpath": {
    "Preview page layout": "//*[@id=\"responsive-preview-navbar-tab\"]/button",
    "main navigation menu": "//nav[@id=\"main_menu\"]"
  }
}
