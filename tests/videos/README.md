# Test videos

This directory holds **generated** varbase-e2e (Playwright + Cucumber-js) videos
recorded for failed scenarios (`*.webm`). They are recreated on every test run and
must **not** be committed — `.gitignore` ignores everything here except this
`README.md`, which keeps the directory in the repository.
