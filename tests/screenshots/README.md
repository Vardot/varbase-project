# Test screenshots

This directory holds **generated** webship-js (Playwright + Cucumber-js) screenshots
captured on failed scenarios (`failed_*.png` / `failed_*.html`). They are recreated on
every test run and must **not** be committed — `.gitignore` ignores everything here
except this `README.md`, which keeps the directory in the repository.
